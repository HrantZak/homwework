{
  "private": true,
  "type": "module",
  "scripts": { "test": "firebase emulators:exec --only firestore --project demo-uspevai-chat --config firebase.json \"node --test rules.test.mjs\"" },
  "devDependencies": {
    "@firebase/rules-unit-testing": "4.0.1",
    "firebase": "11.10.0",
    "firebase-tools": "14.12.0"
  }
}
